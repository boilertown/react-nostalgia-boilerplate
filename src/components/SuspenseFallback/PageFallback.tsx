import { SingleColumnLayout } from 'components/Layouts';

export const PageFallback = () => {
	return (
		<SingleColumnLayout
			renderHeader={null}
			renderMain={null}
			renderFooter={null}
		/>
	);
};
