# 1. Duplicate this file as `.env` file and add values.
# 2. Edit it to fit your project.
# 3. After that, edit env types at file `src/types/process.d.ts`.

# 'development' | 'production'
ENVIRONMENT=

# webpack analyzer, see `webpack.prod.js`
WEBPACK_ANALYZER=
